<h1>Products</h1>

<%= link_to "New product", new_product_path if authenticated? %>

<div id="products">
  <% @products.each do |p| %>
    <div>
      <%= link_to p.name, p %>
    </div>
  <% end %>
</div>

